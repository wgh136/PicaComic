flutter_app: 
  command: pica_comic
  arch: x64
  parent: /usr/local/lib
  nonInteractive: true
  execFieldCodes: u

control:
  Package: pica-comic
  Version: {{Version}}
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer: nyne
  Description: pica comic
  
#options:
#  exec_out_dir: debian/packages